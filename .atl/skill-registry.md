# Skill Registry — SkillMaker

> Auto-generated by sdd-init. Orchestrator caches this once per session and injects compact rules into sub-agent prompts as `## Project Standards (auto-resolved)`. Sub-agents do NOT read this file directly.

## User Skills (selected by code context + task)

| Skill | Trigger | Apply when |
|-------|---------|------------|
| `go-testing` | Go test files, testify/teatest, `*_test.go` | Writing or modifying tests in `backend/` |
| `frontend-design` | Component creation, UI work, HTML/CSS/Angular templates | Any visual/UI work in `frontend/src/app/` |
| `brainstorming` | New feature requests, requirements gathering | Before designing a new feature |
| `skill-creator` | Creating new agent skills | When adding agent instructions |
| `issue-creation` | Creating GitHub issues | When reporting a bug or requesting a feature |
| `branch-pr` | Opening PRs | When creating a pull request |
| `judgment-day` | Adversarial review | When user asks for "judgment day" or dual review |

## Project Conventions (global)

Source: `~/.claude/CLAUDE.md` — personal global instructions. Always loaded. Key rules:

- **Conventional commits only**, NEVER add "Co-Authored-By" or AI attribution.
- **Never build after changes** unless asked.
- **STOP and wait** when asking a question.
- **Verify before agreeing** with user claims; cite evidence.
- **Propose alternatives with tradeoffs** when relevant.
- **Tone**: Senior Architect mentor, passionate teacher, direct but caring. Concepts > Code.
- **Language**: Always match user's language (es for this project).
- **Auto-load `go-testing`** when writing Go tests or Bubbletea TUI tests.

## Compact Rules (injected verbatim into sub-agent prompts)

### When touching `backend/` (Go code)

```
## Project Standards (auto-resolved) — Go backend

- Stack: Go 1.23 + Gin + GORM + golang-migrate + Postgres 16
- Pattern: monolito modular (cmd/api/main.go + internal/modules/<modulo>/{handler,service,repository,domain,dto})
- 7 modulos fijos: auth, users, courses, evaluations, approvals, certificates, reporting
- Cada modulo expone una `Service` interface en `<modulo>.go`; otros modulos dependen de la interface, nunca de structs
- Errores de dominio como `var ErrXxx = errors.New(...)`, traducidos a HTTP en handler con `errors.Is`
- Handlers DELGADOS: parse → call service → render. Cero logica de dominio en handler/
- Repository es el unico lugar con queries GORM. Service nunca toca *gorm.DB
- Siempre `WithContext(ctx)` en queries. Sin contexto = bug
- Migraciones SQL versionadas (golang-migrate up.sql/down.sql). PROHIBIDO AutoMigrate en prod
- Borrado logico (campo `activo`/`estado`), no `gorm.DeletedAt` ni DELETE fisico
- Transacciones SOLO intra-modulo. Cross-modulo = secuencia con compensacion
- Auth: Google OAuth ID token → JWT propio (HS256, 1h) + refresh token opaco (7d) con rotacion
- RBAC: solo 4 roles fijos (alumno|creador|supervisor|administrador). Sin permisos granulares
- Tests: testify para mocks, testcontainers-go para integracion con build tag `integration`
- Logger: log/slog con JSON en prod, request_id correlacionable
- Config tipada con caarlos0/env; secrets `required` fallan en arranque
- Convenciones: paquetes lowercase, tipos PascalCase, archivos snake_case, errores con prefijo Err
- Tabla SQL en singular (user, course, evaluation), Modelos Go en PascalCase singular
- JSON: snake_case para tokens (OAuth spec), camelCase para resto del dominio

Ver `backend/GUIA-BACK.md` para detalle completo.
```

### When touching `frontend/` (Angular code)

```
## Project Standards (auto-resolved) — Angular frontend

- Stack: Angular 21 standalone + zoneless, PrimeNG 21, Tailwind 4, Vitest, SASS
- NO NgModules, NO Zone.js. Change detection via Signals
- Auth: Google Identity Services (GIS) en index.html → POST /api/auth/google → JWT propio
- NUNCA email/password ni register/recover. Solo Google OAuth (RT-12, RNF-03)
- Refresh tokens con rotacion: persistir el NUEVO refresh_token de cada response de /auth/refresh
- 4 roles fijos: 'alumno' | 'creador' | 'supervisor' | 'administrador'. NO permisos granulares
- Guards: `authGuard`, `guestGuard`, `roleGuard`. Funcionales (CanActivateFn), exportados como const
- Interceptors: `authTokenInterceptor` (Bearer), `authRefreshInterceptor` (401 → refresh → retry). Funcionales
- Estructura: `pages/` (lazy loaded), `core/services/[entidad]Service/{service,req.dto,res.dto}.ts`, `shared/`
- Casing: kebab-case archivos, camelCase TS, UserRole type fijo, signals para estado reactivo
- HTTP siempre via HttpPromiseBuilderService (builder pattern, async/await)
- Dialogos/toasts via UiDialogService (wrapper de PrimeNG)
- Sub-routers por rol en pages/platform/{creator,admin,supervisor}/
- Material adjunto: subir directo a S3/MinIO via URL prefirmada (no pasa por el backend)
- Videos: VideoEmbed component con sanitization, NUNCA archivos locales (RT-24)
- Path aliases: @core/*, @pages/*, @shared/*
- environment.ts contiene googleClientId (publico) y googleHostedDomain

Ver `frontend/GUIA-FRONT.md` para detalle completo.
```

### When touching monorepo infra (Makefile, docker, deploy)

```
## Project Standards (auto-resolved) — Monorepo infra

- NO npm workspaces (polyglot Go + npm). Orquestador es Makefile en raiz
- Targets: install/dev/build/test/lint con prefijos backend-* y frontend-* delegan a workspace
- Docker: 4 servicios en prod (postgres, minio, migrate, backend, frontend nginx)
- Migraciones via job `migrate` separado (golang-migrate), backend depends_on service_completed_successfully
- Dockerfile backend: multi-stage Go + distroless/static (CGO_ENABLED=0). Imagen final ~20MB
- Dockerfile frontend: multi-stage Angular AOT + nginx
- Nginx hace reverse proxy /api/* → backend:3000 y SPA fallback
- Dev: backend con Air (hot reload), frontend con ng serve, postgres + minio en docker-compose.dev.yml
- Secrets: SIEMPRE via env vars, NUNCA en repo. .env y backend/.env en .gitignore
- Container env: STORAGE_*, GOOGLE_*, JWT_SECRET, JWT_EXPIRES_IN=1h, REFRESH_TOKEN_EXPIRES_IN=168h

Ver `GUIA-MONOREPO.md` y `SETUP.md`.
```

### When opening PRs / commits

```
## Project Standards (auto-resolved) — Git/PR

- Conventional Commits: feat|fix|chore|docs|refactor|test|style|perf(scope?): mensaje
- NUNCA agregar "Co-Authored-By" o atribucion AI
- Branches: feature/<descripcion>, fix/<descripcion>
- PRs: titulo conciso (<70 chars), body con Summary + Test plan
- Antes de PR: `make lint && make test` localmente
```

## Project Conventions Files Indexed

- `/home/dev/.claude/CLAUDE.md` — Personal global instructions (always loaded)
- `GUIA-MONOREPO.md` — Estructura del monorepo
- `frontend/GUIA-FRONT.md` — Patrones Angular
- `backend/GUIA-BACK.md` — Patrones Go + Gin
- `SETUP.md` — Onboarding del entorno
- `bases/documentacion/*.docx` — Fuente de verdad (RFs, RTs, modelo de datos)
